/* const all = document.getElementsByTagName = '*'

const checkForBig = $.each($('*'), function() { if ($(this).width() > $('body').width()) { console.log($(this).get(0)); } }).length;

console.log(checkForBig) */